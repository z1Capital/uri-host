version: '3.3'

services:
  app:
    build: ./app
    restart: unless-stopped
    env_file: ./.env
    volumes:
      - ./assets:/app/app/assets
    ports:
      - $NODE_LOCAL_PORT:$NODE_DOCKER_PORT
      - 443:443
    stdin_open: true
    tty: true
